<section class="mx-auto max-w-2xl px-4 py-10">
  <h1 class="text-2xl font-bold text-slate-900 dark:text-white mb-4"><%= title %></h1>

  <% if (typeof success !== 'undefined') { %>
    <div class="mb-4 rounded-lg bg-emerald-100 text-emerald-800 px-4 py-3">
      <%= success %>
    </div>
  <% } %>

  <form method="POST" action="/admin/cleanup-duplicates"
        onsubmit="return confirm('Är du säker på att du vill rensa alla dubletter?');">
    <button type="submit"
            class="inline-flex items-center gap-2 rounded-lg bg-rose-600 text-white px-4 py-2 font-semibold hover:bg-rose-700">
      <i class="ti ti-database-cog"></i> Rensa dubletter nu
    </button>
  </form>
</section>