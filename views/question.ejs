<section class="mx-auto max-w-6xl px-4 py-10 grid lg:grid-cols-3 gap-10">
  <!-- Vänsterkolumn -->
  <div class="lg:col-span-2 space-y-8">

    <!-- FRÅGA (eget kort) -->
    <article class="bg-white rounded-2xl border border-slate-200 shadow-sm p-8">
      <div class="text-xs uppercase tracking-wide text-slate-500 mb-2">Fråga</div>

      <h1 class="text-2xl font-bold text-slate-900 mb-2">
        Fråga: <%= q.title %>
      </h1>

      <% if (q.body) { %>
        <p class="text-slate-700 whitespace-pre-line"><%= q.body %></p>
      <% } else { %>
        <p class="text-slate-500 italic">Ingen beskrivning bifogad.</p>
      <% } %>

      <div class="mt-4 text-sm text-slate-500">
        Från: <span class="font-medium"><%= q.user_name || 'Anonym' %></span>
        • Skapad: <%= new Date(q.created_at).toLocaleString('sv-SE') %>
        <% if (q.updated_at && q.updated_at !== q.created_at) { %>
          • Uppdaterad: <%= new Date(q.updated_at).toLocaleString('sv-SE') %>
        <% } %>
      </div>
    </article>

    <!-- SVAR (eget kort under frågan) -->
    <article class="bg-white rounded-2xl border border-slate-200 shadow-sm p-8">
      <div class="flex items-start justify-between mb-4">
        <h2 class="text-xl font-semibold text-slate-900">
          <%= answerTopic ? 'Svar: ' + (answerTopic.title || '') : 'Svar' %>
        </h2>

        <% if (answerTopic && user && user.role === 'admin') { %>
          <div class="flex items-center gap-2">
            <a href="/topic/<%= encodeURIComponent(answerTopic.id) %>"
               class="inline-flex items-center gap-2 rounded-xl bg-slate-900 text-white px-4 py-2 text-sm font-semibold hover:bg-slate-800">
              <i class="ti ti-eye"></i> Visa ämne
            </a>
          </div>
        <% } %>
      </div>

      <% if (answerTopic) { %>
        <div class="prose prose-slate max-w-none mb-6">
          <%- answerTopic.body %>
        </div>
        <div class="text-sm text-slate-500">
          Svarat av: <span class="font-medium"><%= answerTopic.author_name || 'Okänd' %></span>
          • <%= new Date(answerTopic.created_at || answerTopic.updated_at || Date.now()).toLocaleString('sv-SE') %>
        </div>

        <% if (answerTopic.tags) { %>
          <div class="mt-8">
            <h3 class="text-sm font-semibold text-slate-500 uppercase">Taggar</h3>
            <div class="mt-2 flex flex-wrap gap-2">
              <% answerTopic.tags.split(',').forEach(function(tag){ %>
                <span class="text-xs rounded-full bg-slate-100 text-slate-700 px-3 py-1"><%= tag.trim() %></span>
              <% }) %>
            </div>
          </div>
        <% } %>
      <% } else { %>
        <div class="rounded-xl border border-slate-200 bg-slate-50 p-5 text-slate-700">
          Frågan har ej blivit besvarad än.
        </div>
      <% } %>
    </article>
  </div>

  <!-- Högerkolumn (sidopanel) -->
  <aside class="space-y-8">
    <div class="bg-white rounded-2xl border border-slate-200 shadow-sm p-6">
      <h2 class="text-lg font-semibold text-slate-900 mb-4">Relaterade frågor</h2>
      <% if (relatedQuestions && relatedQuestions.length) { %>
        <ul class="space-y-3">
          <% relatedQuestions.forEach(function(rq){ %>
            <li><a href="/questions/<%= rq.id %>" class="text-sky-600 hover:underline"><%= rq.title %></a></li>
          <% }) %>
        </ul>
      <% } else { %>
        <p class="text-sm text-slate-500">Inga frågor kopplade än.</p>
      <% } %>
    </div>

    <div class="bg-white rounded-2xl border border-slate-200 shadow-sm p-6">
      <h2 class="text-lg font-semibold text-slate-900 mb-4">Fler ämnen i denna kategori</h2>
      <% if (relatedTopics && relatedTopics.length) { %>
        <ul class="space-y-3">
          <% relatedTopics.forEach(function(rt){ %>
            <li><a href="/topic/<%= rt.id %>" class="text-slate-700 hover:text-sky-700"><%= rt.title %></a></li>
          <% }) %>
        </ul>
      <% } else { %>
        <p class="text-sm text-slate-500">Inga fler ämnen ännu.</p>
      <% } %>
    </div>
  </aside>
</section>